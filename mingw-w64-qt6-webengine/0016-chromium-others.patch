diff -ur a/src/3rdparty/chromium/BUILD.gn b/src/3rdparty/chromium/BUILD.gn
--- a/src/3rdparty/chromium/BUILD.gn	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/BUILD.gn	2024-05-24 20:31:34.628923000 +0800
@@ -1109,7 +1109,7 @@
         "//chrome/test/chromedriver:chromedriver_server",
         "//third_party/blink/tools:wpt_tests_isolate",
       ]
-      if (is_win) {
+      if (is_win && !is_mingw) {
         data_deps += [ "//build/win:copy_cdb_to_output" ]
       }
     }
@@ -1227,7 +1227,9 @@
         "//third_party/apache-windows-arm64/",
         "//third_party/perl/perl/",
       ]
-      data_deps += [ "//build/win:copy_cdb_to_output" ]
+      if (!is_mingw) {
+        data_deps += [ "//build/win:copy_cdb_to_output" ]
+      }
     }
 
     if (is_mac) {
diff -ur a/src/3rdparty/chromium/crypto/BUILD.gn b/src/3rdparty/chromium/crypto/BUILD.gn
--- a/src/3rdparty/chromium/crypto/BUILD.gn	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/crypto/BUILD.gn	2024-05-23 21:32:03.855110300 +0800
@@ -116,7 +116,7 @@
       "scoped_cng_types.h",
       "unexportable_key_win.cc",
     ]
-    libs = [ "ncrypt.lib" ]
+    libs = [ "ncrypt" ]
   }
 
   # Some files are built when NSS is used for the platform certificate library.
diff -ur a/src/3rdparty/chromium/crypto/unexportable_key_win.cc b/src/3rdparty/chromium/crypto/unexportable_key_win.cc
--- a/src/3rdparty/chromium/crypto/unexportable_key_win.cc	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/crypto/unexportable_key_win.cc	2024-05-26 14:50:03.031930100 +0800
@@ -148,6 +148,7 @@
     return absl::nullopt;
   }
 
+#if !defined(COMPILER_GCC) // missing NCRYPT_ECC_CURVE_NAME_PROPERTY
   // Sometimes NCrypt will return a generic dwMagic even when asked for a P-256
   // key. In that case, do extra validation to make sure that `key` is in fact
   // a P-256 key.
@@ -164,6 +165,7 @@
       return absl::nullopt;
     }
   }
+#endif
 
   uint8_t x962[1 + 32 + 32];
   x962[0] = POINT_CONVERSION_UNCOMPRESSED;
@@ -649,6 +651,7 @@
       return nullptr;
     }
 
+#if !defined(COMPILER_GCC) // missing NCRYPT_USE_VIRTUAL_ISOLATION_FLAG
     ScopedNCryptKey key;
     {
       SCOPED_MAY_LOAD_LIBRARY_AT_BACKGROUND_PRIORITY();
@@ -690,6 +693,7 @@
       default:
         return nullptr;
     }
+#endif
   }
 
   std::unique_ptr<VirtualUnexportableSigningKey> FromKeyName(
diff -ur a/src/3rdparty/chromium/skia/BUILD.gn b/src/3rdparty/chromium/skia/BUILD.gn
--- a/src/3rdparty/chromium/skia/BUILD.gn	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/skia/BUILD.gn	2024-05-23 21:38:53.266213500 +0800
@@ -237,7 +237,7 @@
 
 source_set("skcms") {
   cflags = []
-  if (!is_win || is_clang) {
+  if (!is_msvc || is_clang) {
     cflags += [
       "-w",
       "-std=c11",
@@ -422,7 +422,7 @@
   sources -= [ "//third_party/skia/src/utils/SkParsePath.cpp" ]
 
   if (is_win) {
-    libs = [ "fontsub.lib" ]
+    libs = [ "fontsub" ]
   }
 
   # need separate win section to handle chromes auto gn filter
@@ -696,10 +696,16 @@
       configs -= [ "//build/config/compiler:afdo_optimize_size" ]
     }
     if (is_win) {
+      if (is_mingw) {
+        cflags_cc = [
+          "-Wno-missing-field-initializers",
+        ]
+      } else {
       cflags_cc = [
         "/wd5041",  # out-of-line definition for constexpr static data member is
                     # not needed and is deprecated in C++17
       ]
+      }
     }
   }
 }
@@ -730,7 +736,7 @@
 if (current_cpu == "x86" || current_cpu == "x64") {
   skia_source_set("skia_opts_hsw") {
     sources = skia_opts.hsw_sources
-    if (!is_win) {
+    if (!is_msvc) {
       cflags = [
         "-mavx2",
         "-mbmi",
@@ -739,7 +745,7 @@
         "-mfma",
       ]
     }
-    if (is_win) {
+    if (is_msvc) {
       cflags = [ "/arch:AVX2" ]
     }
     visibility = [ ":skia_opts" ]
diff -ur a/src/3rdparty/chromium/sql/sqlite_result_code.cc b/src/3rdparty/chromium/sql/sqlite_result_code.cc
--- a/src/3rdparty/chromium/sql/sqlite_result_code.cc	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/sql/sqlite_result_code.cc	2024-05-23 21:32:04.042609000 +0800
@@ -155,15 +155,15 @@
     {SQLITE_ERROR_SNAPSHOT,
      static_cast<int>(SqliteLoggedResultCode::kUnusedChrome)},
 #ifdef SQLITE_ENABLE_SNAPSHOT
-#error "This code assumes that Chrome does not use sqlite3_snapshot_open()"
+#error This code assumes that Chrome does not use sqlite3_snapshot_open()
 #endif
 
     // Chrome does not use blocking Posix advisory file lock requests.
     {SQLITE_BUSY_TIMEOUT,
      static_cast<int>(SqliteLoggedResultCode::kUnusedChrome)},
 #ifdef SQLITE_ENABLE_SETLK_TIMEOUT
-#error "This code assumes that Chrome does not use blocking Posix advisory \
-file lock requests"
+#error This code assumes that Chrome does not use blocking Posix advisory \
+file lock requests
 #endif
 
     {SQLITE_READONLY_ROLLBACK,
diff -ur a/src/3rdparty/chromium/sql/vfs_wrapper.cc b/src/3rdparty/chromium/sql/vfs_wrapper.cc
--- a/src/3rdparty/chromium/sql/vfs_wrapper.cc	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/sql/vfs_wrapper.cc	2024-05-23 21:32:04.058199700 +0800
@@ -5,6 +5,7 @@
 #include "sql/vfs_wrapper.h"
 
 #include <algorithm>
+#include <cstring>
 #include <string>
 #include <vector>
 
