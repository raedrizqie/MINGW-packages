diff -ur a/src/3rdparty/chromium/third_party/pdfium/BUILD.gn b/src/3rdparty/chromium/third_party/pdfium/BUILD.gn
--- a/src/3rdparty/chromium/third_party/pdfium/BUILD.gn	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/third_party/pdfium/BUILD.gn	2024-05-24 01:56:28.952379500 +0800
@@ -207,6 +207,13 @@
         }
       }
     }
+  } else if (is_mingw) {
+    # Suppress myriad of incomplete enumeration warnings
+    cflags += [ "-Wno-switch" ]
+
+    if (current_cpu == "x86" && !is_clang) {
+      cflags += [ "-msse2" ]
+    }
   }
 
   if (is_ios) {
@@ -224,7 +231,7 @@
     cflags += [ "-Wtautological-unsigned-zero-compare" ]
   }
 
-  if (!is_win && !is_clang) {
+  if (!is_msvc && !is_clang) {
     cflags += [
       # Override -Wno-narrowing for GCC.
       "-Wnarrowing",
diff -ur a/src/3rdparty/chromium/third_party/pdfium/skia/BUILD.gn b/src/3rdparty/chromium/third_party/pdfium/skia/BUILD.gn
--- a/src/3rdparty/chromium/third_party/pdfium/skia/BUILD.gn	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/third_party/pdfium/skia/BUILD.gn	2024-05-24 02:02:26.180340800 +0800
@@ -123,6 +123,7 @@
       "SK_DEFAULT_FONT_CACHE_COUNT_LIMIT=256",
     ]
 
+    if (is_msvc) {
     cflags = [
       "/wd4244",  # conversion from 'type1( __int64)' to 'type2 (unsigned int)'
       "/wd4341",  # signed value is out of range for enum constant.
@@ -136,12 +137,13 @@
       "/wd5041",  # out-of-line definition for constexpr static data member is
                   # not needed and is deprecated in C++17
     ]
+    }
   }
 }
 
 source_set("skcms") {
   cflags = []
-  if (!is_win || is_clang) {
+  if (!is_msvc || is_clang) {
     cflags += [
       "-w",
       "-std=c11",
@@ -400,7 +402,7 @@
     ]
     public_configs = [ ":skia_config" ]
 
-    if (is_win) {
+    if (is_msvc) {
       cflags_cc = [
         "/wd5041",  # out-of-line definition for constexpr static data member is
                     # not needed and is deprecated in C++17
@@ -424,7 +426,7 @@
 if (current_cpu == "x86" || current_cpu == "x64") {
   skia_source_set("skia_opts_hsw") {
     sources = skia_opts.hsw_sources
-    if (!is_win) {
+    if (!is_msvc) {
       cflags = [
         "-mavx2",
         "-mbmi",
@@ -433,7 +435,7 @@
         "-mfma",
       ]
     }
-    if (is_win) {
+    if (is_msvc) {
       cflags = [ "/arch:AVX2" ]
     }
     visibility = [ ":skia_opts" ]
diff -ur a/src/3rdparty/chromium/third_party/pdfium/third_party/BUILD.gn b/src/3rdparty/chromium/third_party/pdfium/third_party/BUILD.gn
--- a/src/3rdparty/chromium/third_party/pdfium/third_party/BUILD.gn	2024-05-15 21:57:21.000000000 +0800
+++ b/src/3rdparty/chromium/third_party/pdfium/third_party/BUILD.gn	2024-05-24 01:56:29.108642600 +0800
@@ -391,7 +391,7 @@
       # Various files convert MAX_INT to double and similar without casting.
       "-Wno-implicit-int-float-conversion",
     ]
-  } else if (is_win && target_cpu == "x86") {
+  } else if (is_msvc && target_cpu == "x86") {
     cflags = [
       # Signed/unsigned comparisons.
       "/wd4018",
@@ -470,6 +470,13 @@
       if (is_win) {
         # Need to undefine the macro since it is redefined in tif_jpeg.c.
         configs -= [ "//build/config/win:lean_and_mean" ]
+        if (is_mingw) {
+          cflags = [
+            "-Wno-integer-overflow",
+            "-Wno-overflow",
+            "-Wno-shift-count-overflow"
+          ]
+        }
       }
       deps = [
         ":zlib",
