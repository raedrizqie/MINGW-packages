diff --git a/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn b/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn
index 6cbb60c..b0d351b 100644
--- a/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/BUILD.gn
@@ -592,7 +592,7 @@ target(link_target_type, "gles2_sources") {
     }
   }
 
-  if (is_win) {
+  if (is_msvc) {
     sources += [
       "dxgi_shared_handle_manager.cc",
       "dxgi_shared_handle_manager.h",
@@ -617,6 +617,16 @@ target(link_target_type, "gles2_sources") {
     ]
     libs = [ "dxguid.lib" ]
   }
+
+  if (is_mingw) {
+    sources += [
+      "shared_image/d3d_image_representation.cc",
+      "shared_image/d3d_image_representation.h",
+      "shared_image/d3d_image_utils.cc",
+      "shared_image/d3d_image_utils.h",
+    ]
+    libs = [ "dxguid" ]
+  }
 }
 
 proto_library("disk_cache_proto") {
diff --git a/src/3rdparty/chromium/gpu/command_buffer/service/gles2_cmd_decoder_passthrough.cc b/src/3rdparty/chromium/gpu/command_buffer/service/gles2_cmd_decoder_passthrough.cc
index 28fa9e0..815a8e9 100644
--- a/src/3rdparty/chromium/gpu/command_buffer/service/gles2_cmd_decoder_passthrough.cc
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/gles2_cmd_decoder_passthrough.cc
@@ -1420,12 +1420,12 @@ gpu::Capabilities GLES2DecoderPassthroughImpl::GetCapabilities() {
       feature_info_->workarounds().avoid_stencil_buffers;
   caps.multisample_compatibility =
       feature_info_->feature_flags().ext_multisample_compatibility;
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   caps.shared_image_d3d = D3DImageBackingFactory::IsD3DSharedImageSupported(
       group_->gpu_preferences());
   caps.shared_image_swap_chain =
       caps.shared_image_d3d && D3DImageBackingFactory::IsSwapChainSupported();
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
   if (base::FeatureList::IsEnabled(features::kPassthroughYuvRgbConversion)) {
     caps.supports_yuv_rgb_conversion = true;
   }
diff --git a/src/3rdparty/chromium/gpu/command_buffer/service/raster_decoder.cc b/src/3rdparty/chromium/gpu/command_buffer/service/raster_decoder.cc
index a7eaf5b..ac964d7 100644
--- a/src/3rdparty/chromium/gpu/command_buffer/service/raster_decoder.cc
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/raster_decoder.cc
@@ -1280,11 +1280,11 @@ Capabilities RasterDecoderImpl::GetCapabilities() {
     caps.texture_half_float_linear =
         feature_info()->feature_flags().enable_texture_half_float_linear;
   }
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   caps.shared_image_d3d =
       D3DImageBackingFactory::IsD3DSharedImageSupported(gpu_preferences_);
   caps.shared_image_swap_chain = D3DImageBackingFactory::IsSwapChainSupported();
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
   caps.disable_legacy_mailbox = disable_legacy_mailbox_;
   // TODO(crbug.com/1450879): Support YUV rendering and readback for Graphite.
   caps.supports_yuv_rgb_conversion = !graphite_context();
diff --git a/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_factory.cc b/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_factory.cc
index b2aef19..a1c9af1 100644
--- a/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_factory.cc
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_factory.cc
@@ -269,7 +269,7 @@ SharedImageFactory::SharedImageFactory(
     factories_.push_back(std::move(gl_texture_backing_factory));
   }
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   if (gl::DirectCompositionSupported()) {
     factories_.push_back(std::make_unique<DCompImageBackingFactory>(
         shared_context_state_.get()));
@@ -282,7 +282,7 @@ SharedImageFactory::SharedImageFactory(
     d3d_backing_factory_ = d3d_factory.get();
     factories_.push_back(std::move(d3d_factory));
   }
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
 
 #if !BUILDFLAG(IS_ANDROID)
   if (use_gl) {
@@ -770,7 +770,7 @@ void SharedImageFactory::DestroyAllSharedImages(bool have_context) {
   shared_images_.clear();
 }
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
 bool SharedImageFactory::CreateSwapChain(const Mailbox& front_buffer_mailbox,
                                          const Mailbox& back_buffer_mailbox,
                                          viz::SharedImageFormat format,
@@ -800,7 +800,7 @@ bool SharedImageFactory::PresentSwapChain(const Mailbox& mailbox) {
   (*it)->PresentSwapChain();
   return true;
 }
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
 
 #if BUILDFLAG(IS_FUCHSIA)
 void SharedImageFactory::RegisterSysmemBufferCollection(
diff --git a/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.cc b/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.cc
index 971a0f0..d5cf859 100644
--- a/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.cc
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.cc
@@ -23,7 +23,7 @@
 #include "gpu/command_buffer/service/shared_image/shared_image_representation.h"
 #include "ui/gl/trace_util.h"
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
 #include "gpu/command_buffer/service/dxgi_shared_handle_manager.h"
 #include "ui/gl/gl_angle_util_win.h"
 #endif
@@ -82,7 +82,7 @@ class SCOPED_LOCKABLE SharedImageManager::AutoLock {
 SharedImageManager::SharedImageManager(bool thread_safe,
                                        bool display_context_on_another_thread)
     : display_context_on_another_thread_(display_context_on_another_thread)
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
       ,
       dxgi_shared_handle_manager_(
           base::MakeRefCounted<DXGISharedHandleManager>())
diff --git a/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.h b/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.h
index 4b61f93..c15a4ad 100644
--- a/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.h
+++ b/src/3rdparty/chromium/gpu/command_buffer/service/shared_image/shared_image_manager.h
@@ -128,7 +128,7 @@ class GPU_GLES2_EXPORT SharedImageManager
   // method is to facilitate pageflip testing on the viz thread.
   scoped_refptr<gfx::NativePixmap> GetNativePixmap(const gpu::Mailbox& mailbox);
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   const scoped_refptr<DXGISharedHandleManager>& dxgi_shared_handle_manager()
       const {
     return dxgi_shared_handle_manager_;
@@ -146,7 +146,7 @@ class GPU_GLES2_EXPORT SharedImageManager
 
   bool is_registered_as_memory_dump_provider_ = false;
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   scoped_refptr<DXGISharedHandleManager> dxgi_shared_handle_manager_;
 #endif
 
diff --git a/src/3rdparty/chromium/gpu/config/BUILD.gn b/src/3rdparty/chromium/gpu/config/BUILD.gn
index 9b64ef6..b3fff22 100644
--- a/src/3rdparty/chromium/gpu/config/BUILD.gn
+++ b/src/3rdparty/chromium/gpu/config/BUILD.gn
@@ -223,8 +223,8 @@ source_set("config_sources") {
       "gpu_info_collector_win.cc",
     ]
     libs = [
-      "dxgi.lib",
-      "dxguid.lib",
+      "dxgi",
+      "dxguid",
     ]
 
     if (is_chrome_branded && is_official_build) {
diff --git a/src/3rdparty/chromium/gpu/config/device_perf_info.h b/src/3rdparty/chromium/gpu/config/device_perf_info.h
index bdcdbc4..472b3b3 100644
--- a/src/3rdparty/chromium/gpu/config/device_perf_info.h
+++ b/src/3rdparty/chromium/gpu/config/device_perf_info.h
@@ -46,7 +46,7 @@ struct GPU_EXPORT DevicePerfInfo {
   uint32_t total_physical_memory_mb = 0u;
   uint32_t total_disk_space_mb = 0u;
   uint32_t hardware_concurrency = 0u;
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   // system commit limit (n pages) x page size.
   uint32_t system_commit_limit_mb = 0u;
   // If multiple GPUs are detected, this holds the highest feature level.
diff --git a/src/3rdparty/chromium/gpu/config/gpu_driver_bug_workarounds.h b/src/3rdparty/chromium/gpu/config/gpu_driver_bug_workarounds.h
index 1951c8b..befb28a 100644
--- a/src/3rdparty/chromium/gpu/config/gpu_driver_bug_workarounds.h
+++ b/src/3rdparty/chromium/gpu/config/gpu_driver_bug_workarounds.h
@@ -5,6 +5,7 @@
 #ifndef GPU_CONFIG_GPU_DRIVER_BUG_WORKAROUNDS_H_
 #define GPU_CONFIG_GPU_DRIVER_BUG_WORKAROUNDS_H_
 
+#include <cstdint>
 #include <vector>
 
 #include "build/build_config.h"
diff --git a/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc b/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc
index fd4c295..f6e46ea 100644
--- a/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc
+++ b/src/3rdparty/chromium/gpu/config/gpu_info_collector_win.cc
@@ -329,6 +329,7 @@ void GetGpuSupportedD3D12Version(uint32_t& d3d12_feature_level,
       return;
     }
 
+#if defined(COMPILER_MSVC)
     // For the default adapter only: EnumAdapters(0, ...).
     // Check to see if the adapter supports Direct3D 12.
     for (auto level : feature_levels) {
@@ -338,8 +339,10 @@ void GetGpuSupportedD3D12Version(uint32_t& d3d12_feature_level,
         break;
       }
     }
+#endif
   }
 
+#if defined(COMPILER_MSVC)
   // Query the maximum supported shader model version.
   if (d3d12_device) {
     // As per the documentation, CheckFeatureSupport will return E_INVALIDARG if
@@ -363,6 +366,7 @@ void GetGpuSupportedD3D12Version(uint32_t& d3d12_feature_level,
       }
     }
   }
+#endif
 }
 
 // The old graphics drivers are installed to the Windows system directory
@@ -596,6 +600,7 @@ void RecordGpuSupportedDx12VersionHistograms(
       ConvertToHistogramShaderVersion(highest_shader_model_version));
 }
 
+#if defined(COMPILER_MSVC)
 bool CollectD3D11FeatureInfo(D3D_FEATURE_LEVEL* d3d11_feature_level,
                              bool* has_discrete_gpu) {
   Microsoft::WRL::ComPtr<IDXGIFactory1> dxgi_factory;
@@ -662,6 +667,7 @@ bool CollectD3D11FeatureInfo(D3D_FEATURE_LEVEL* d3d11_feature_level,
   }
   return false;
 }
+#endif
 
 bool CollectContextGraphicsInfo(GPUInfo* gpu_info) {
   TRACE_EVENT0("gpu", "CollectGraphicsInfo");
diff --git a/src/3rdparty/chromium/gpu/config/gpu_util.cc b/src/3rdparty/chromium/gpu/config/gpu_util.cc
index 87e0039..fb4c2df 100644
--- a/src/3rdparty/chromium/gpu/config/gpu_util.cc
+++ b/src/3rdparty/chromium/gpu/config/gpu_util.cc
@@ -404,7 +404,7 @@ void RecordGpuHistogram(uint32_t vendor_id, uint32_t device_id) {
   }
 }
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
 uint32_t GetSystemCommitLimitMb() {
   PERFORMANCE_INFORMATION perf_info = {sizeof(perf_info)};
   if (::GetPerformanceInfo(&perf_info, sizeof(perf_info))) {
@@ -415,7 +415,7 @@ uint32_t GetSystemCommitLimitMb() {
   }
   return 0u;
 }
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
 
 #if BUILDFLAG(IS_ANDROID)
 GPUInfo* g_gpu_info_cache = nullptr;
@@ -1005,7 +1005,7 @@ void CollectDevicePerfInfo(DevicePerfInfo* device_perf_info,
   device_perf_info->hardware_concurrency =
       static_cast<uint32_t>(std::thread::hardware_concurrency());
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   device_perf_info->system_commit_limit_mb = GetSystemCommitLimitMb();
   if (!in_browser_process) {
     D3D_FEATURE_LEVEL d3d11_feature_level = D3D_FEATURE_LEVEL_1_0_CORE;
@@ -1027,7 +1027,7 @@ void RecordDevicePerfInfoHistograms() {
                             device_perf_info->total_disk_space_mb / 1024);
   UMA_HISTOGRAM_COUNTS_100("Hardware.Concurrency",
                            device_perf_info->hardware_concurrency);
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   UMA_HISTOGRAM_COUNTS_100("Memory.Total.SystemCommitLimit",
                            device_perf_info->system_commit_limit_mb / 1024);
   UMA_HISTOGRAM_ENUMERATION("GPU.D3D11FeatureLevel",
@@ -1035,7 +1035,7 @@ void RecordDevicePerfInfoHistograms() {
                                 device_perf_info->d3d11_feature_level));
   UMA_HISTOGRAM_ENUMERATION("GPU.HasDiscreteGpu",
                             device_perf_info->has_discrete_gpu);
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
   UMA_HISTOGRAM_ENUMERATION("GPU.IntelGpuGeneration",
                             device_perf_info->intel_gpu_generation);
   UMA_HISTOGRAM_BOOLEAN("GPU.SoftwareRendering",
diff --git a/src/3rdparty/chromium/gpu/ipc/common/BUILD.gn b/src/3rdparty/chromium/gpu/ipc/common/BUILD.gn
index 04ca158..86b4067 100644
--- a/src/3rdparty/chromium/gpu/ipc/common/BUILD.gn
+++ b/src/3rdparty/chromium/gpu/ipc/common/BUILD.gn
@@ -101,7 +101,7 @@ source_set("ipc_common_sources") {
       "gpu_memory_buffer_impl_dxgi.cc",
       "gpu_memory_buffer_impl_dxgi.h",
     ]
-    libs = [ "d3d11.lib" ]
+    libs = [ "d3d11" ]
   }
   if (use_ozone) {
     sources += [
diff --git a/src/3rdparty/chromium/gpu/ipc/common/device_perf_info.mojom b/src/3rdparty/chromium/gpu/ipc/common/device_perf_info.mojom
index 326b53e..0987f3f 100644
--- a/src/3rdparty/chromium/gpu/ipc/common/device_perf_info.mojom
+++ b/src/3rdparty/chromium/gpu/ipc/common/device_perf_info.mojom
@@ -32,10 +32,10 @@ struct DevicePerfInfo {
   uint32 total_disk_space_mb;
   uint32 hardware_concurrency;
 
-  [EnableIf=is_win]
+  [EnableIf=is_msvc]
   uint32 system_commit_limit_mb;
-  [EnableIf=is_win]
+  [EnableIf=is_msvc]
   Direct3DFeatureLevel d3d11_feature_level;
-  [EnableIf=is_win]
+  [EnableIf=is_msvc]
   HasDiscreteGpu has_discrete_gpu;
 };
diff --git a/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.cc b/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.cc
index c8a9793..d4eab25 100644
--- a/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.cc
+++ b/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.cc
@@ -127,11 +127,11 @@ bool StructTraits<gpu::mojom::DevicePerfInfoDataView, gpu::DevicePerfInfo>::
   out->total_disk_space_mb = data.total_disk_space_mb();
   out->hardware_concurrency = data.hardware_concurrency();
   bool rt = true;
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   out->system_commit_limit_mb = data.system_commit_limit_mb();
   rt &= data.ReadD3d11FeatureLevel(&out->d3d11_feature_level);
   rt &= data.ReadHasDiscreteGpu(&out->has_discrete_gpu);
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
   return rt;
 }
 
diff --git a/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.h b/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.h
index 0dc84dc..a584cf6 100644
--- a/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.h
+++ b/src/3rdparty/chromium/gpu/ipc/common/device_perf_info_mojom_traits.h
@@ -50,7 +50,7 @@ struct GPU_EXPORT
     return info.hardware_concurrency;
   }
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
   static uint32_t system_commit_limit_mb(const gpu::DevicePerfInfo& info) {
     return info.system_commit_limit_mb;
   }
diff --git a/src/3rdparty/chromium/gpu/ipc/service/BUILD.gn b/src/3rdparty/chromium/gpu/ipc/service/BUILD.gn
index 090bbd6..eca3b5d 100644
--- a/src/3rdparty/chromium/gpu/ipc/service/BUILD.gn
+++ b/src/3rdparty/chromium/gpu/ipc/service/BUILD.gn
@@ -100,9 +100,9 @@ component("service") {
       "image_transport_surface_win.cc",
     ]
     libs += [
-      "d3d11.lib",
-      "dcomp.lib",
-      "dxguid.lib",
+      "d3d11",
+      "dcomp",
+      "dxguid",
     ]
   }
   if (is_apple) {
diff --git a/src/3rdparty/chromium/gpu/ipc/service/shared_image_stub.cc b/src/3rdparty/chromium/gpu/ipc/service/shared_image_stub.cc
index 704b004..72bd148 100644
--- a/src/3rdparty/chromium/gpu/ipc/service/shared_image_stub.cc
+++ b/src/3rdparty/chromium/gpu/ipc/service/shared_image_stub.cc
@@ -129,7 +129,7 @@ void SharedImageStub::ExecuteDeferredRequest(
       OnDestroySharedImage(request->get_destroy_shared_image());
       break;
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
     case mojom::DeferredSharedImageRequest::Tag::kCopyToGpuMemoryBuffer: {
       auto& params = *request->get_copy_to_gpu_memory_buffer();
       OnCopyToGpuMemoryBuffer(params.mailbox, params.release_id);
@@ -144,7 +144,7 @@ void SharedImageStub::ExecuteDeferredRequest(
       OnPresentSwapChain(request->get_present_swap_chain()->mailbox,
                          request->get_present_swap_chain()->release_id);
       break;
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
   }
 }
 
@@ -483,7 +483,7 @@ void SharedImageStub::OnDestroySharedImage(const Mailbox& mailbox) {
   }
 }
 
-#if BUILDFLAG(IS_WIN)
+#if defined(COMPILER_MSVC)
 void SharedImageStub::OnCopyToGpuMemoryBuffer(const Mailbox& mailbox,
                                               uint32_t release_id) {
   if (!mailbox.IsSharedImage()) {
@@ -557,7 +557,7 @@ void SharedImageStub::OnPresentSwapChain(const Mailbox& mailbox,
 
   sync_point_client_state_->ReleaseFenceSync(release_id);
 }
-#endif  // BUILDFLAG(IS_WIN)
+#endif  // defined(COMPILER_MSVC)
 
 #if BUILDFLAG(IS_FUCHSIA)
 void SharedImageStub::RegisterSysmemBufferCollection(
